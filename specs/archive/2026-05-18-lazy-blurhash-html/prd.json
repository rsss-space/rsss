{
  "project": "RSSS",
  "branchName": "ralph/lazy-blurhash-html",
  "description": "Lazy Per-User BlurHash HTML - Generate per-user index.html on first request with first-page items and blurhash strings embedded as a JSON bootstrap, cached in KV keyed by a DO-tracked feed_version. Cache invalidates via version bumps when ingest writes new items or new blurhash data. Includes prior cache-indicator and blurhash-infrastructure work.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Show cache indicator on all authenticated routes",
      "description": "As a logged-in user, I want the cache status indicator visible on every authenticated page so I can monitor my cache state without navigating back to a feed.",
      "acceptanceCriteria": [
        "Remove the `isFeedReaderRoute` guard from the visibility gate in `src/client/components/cache-status.ts`",
        "Delete the `isFeedReaderRoute` helper if it has no other callers",
        "Indicator renders on `/`, `/starred`, `/feed/*`, `/settings`, `/about`, `/add-feed`, and any other authenticated route",
        "Indicator does not render on unauthenticated routes (gated by `state.user.value`) — existing behavior preserved",
        "Existing 'free / non-entitled user' hide behavior preserved",
        "Existing 'no local DB / not bootstrapped' hide behavior preserved",
        "No errors on routes where `state.selectedFeedId` is null (recompute logic already handles `feedId: null`)",
        "Typecheck passes",
        "Lint passes",
        "Verify in browser using dev-browser skill: indicator appears on `/settings` and `/about` in addition to feed reader routes"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Hide cache indicator when sync or article caching is off",
      "description": "As a user who has turned off local-first sync or article caching, I want the cache indicator to disappear so it doesn't falsely claim my content is cached.",
      "acceptanceCriteria": [
        "In `src/client/components/cache-status.ts`, import `syncSubscriptions` and `storeContent` signals from `../local-first-settings.js`",
        "Extend the visibility gate to return null when `syncSubscriptions.value` is false",
        "Extend the visibility gate to return null when `storeContent.value` is false (even if `syncSubscriptions` is true)",
        "Component subscribes to both signals so toggling either in `/settings` immediately updates the header without a page refresh",
        "If sequentially setting multiple signals anywhere as part of this change, use `batch()` from `@preact/signals`",
        "Lines stay within 80 columns",
        "Typecheck passes",
        "Lint passes",
        "Verify in browser using dev-browser skill: toggle off 'Sync subscriptions and read state to this device' → indicator disappears; toggle off 'Store article content locally' with sync on → indicator disappears"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Show neutral 'No items yet' state when totalCount is 0",
      "description": "As a user with sync and article caching enabled but no items locally yet, I want a neutral indicator rather than a misleading '100% cached' so I understand there is nothing actually cached yet.",
      "acceptanceCriteria": [
        "In `src/client/components/cache-status.ts`, split the existing `uncachedCount === 0` branch into two branches: `totalCount === 0` (neutral) and `totalCount > 0 && uncachedCount === 0` (existing green '100% cached')",
        "Neutral state renders a gray dot and label 'No items yet'",
        "Neutral state reuses the existing `Dot` component; if a gray color variant does not exist, add it minimally and reuse an existing CSS variable from `_variables.css` (do not introduce new color tokens unnecessarily)",
        "Neutral state has `aria-label` such as 'Cache status: no items yet' for screen readers",
        "Neutral state uses the same `cache-status-legend` markup as the synced state for consistent layout",
        "Neutral state is non-interactive in v1",
        "When `totalCount > 0 && uncachedCount === 0`, the existing green '100% cached' state still renders unchanged",
        "When `uncachedCount > 0`, the existing yellow 'N uncached' state with action popover still renders unchanged",
        "Update the comment in `src/client/components/cache-status-state.ts` (around lines 67-73) about 'EMPTY_SNAPSHOT' from '100% cached' to 'neutral / hidden' for accuracy",
        "Lines stay within 80 columns",
        "Do not change unrelated CSS",
        "Typecheck passes",
        "Lint passes",
        "Verify in browser using dev-browser skill: with sync + article cache enabled and no items yet, header shows the gray 'No items yet' state, not green '100% cached'"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "Verify header indicator and /settings agree across all states",
      "description": "As a user, I want the header indicator and the `/settings` cache section to never contradict each other so I can trust both views.",
      "acceptanceCriteria": [
        "When `/settings` shows 'Total storage used: 0 B' with both toggles off, the header has no cache indicator at all",
        "When `/settings` shows both toggles on with no items cached, the header shows the neutral 'No items yet' state",
        "When `/settings` shows both toggles on with items partially cached, the header shows the yellow 'N uncached' state with action button (existing behavior)",
        "When `/settings` shows both toggles on with all items cached, the header shows green '100% cached' (existing behavior)",
        "Typecheck passes",
        "Lint passes",
        "Verify in browser using dev-browser skill: walk through all four state combinations and confirm header/settings agree in each"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Add tests for new visibility and empty-state rules",
      "description": "As a developer, I want unit/integration tests that encode the new visibility and empty-state rules so future regressions are caught automatically.",
      "acceptanceCriteria": [
        "Tests in `test/cache-status.ts` cover: indicator hidden when `syncSubscriptions` is false",
        "Tests cover: indicator hidden when `storeContent` is false (with `syncSubscriptions` true)",
        "Tests cover: indicator shows neutral empty state when `totalCount` is 0 with both toggles on",
        "Tests cover: indicator renders on `/settings` and `/about` routes (not just feed reader routes)",
        "Tests cover: existing behavior preserved — yellow 'N uncached' when `uncachedCount > 0`, green '100% cached' when `totalCount > 0 && uncachedCount === 0`",
        "`npm test` passes",
        "`npm run lint` passes",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "Add og_image_url, blurhash, image_width, image_height columns to feed_items",
      "description": "As a developer, I need columns on `feed_items` to denormalize the OG image URL, its BlurHash, and its intrinsic dimensions so the render path is a single SELECT with no per-item KV lookup.",
      "acceptanceCriteria": [
        "Migration adds `og_image_url TEXT NULL` to `feed_items`",
        "Migration adds `blurhash TEXT NULL` to `feed_items`",
        "Migration adds `image_width INTEGER NULL` to `feed_items`",
        "Migration adds `image_height INTEGER NULL` to `feed_items`",
        "No index added on the new columns (read with the row, never filtered on)",
        "Migration applies cleanly on a fresh DB (no existing rows)",
        "Migration applies cleanly on an existing DB with rows (existing rows take NULL defaults)",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 6,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "Provision BLURHASH_KV namespace and blurhash-jobs queue infrastructure",
      "description": "As a developer, I need the KV namespace and queue (with DLQ) declared in wrangler configuration so the producer (poller) and consumer workers can bind to them.",
      "acceptanceCriteria": [
        "`BLURHASH_KV` KV namespace declared in `wrangler.toml` (or equivalent)",
        "`BLURHASH_KV` bound for both poller and consumer workers",
        "`blurhash-jobs` queue declared as a producer binding for the poller",
        "`blurhash-jobs` queue declared as a consumer binding with `max_batch_size = 10`, `max_batch_timeout = 30`, `max_retries = 3`",
        "`blurhash-dlq` declared as the `dead_letter_queue` for `blurhash-jobs`",
        "`wrangler types` (or project equivalent) regenerated and types compile",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-008",
      "title": "Extract og:image in feed poller and persist og_image_url",
      "description": "As a developer, I need the existing feed poller to extract the og:image meta tag from each new item's article URL and write it to `og_image_url`, so subsequent stages have a URL to hash.",
      "acceptanceCriteria": [
        "When the poller ingests a new item, it fetches the article URL and parses `<meta property=\"og:image\">`",
        "Falls back to `<meta name=\"og:image\">` if the property form is absent",
        "If the feed payload itself carries an image (e.g. `media:thumbnail`, `enclosure`), prefer that to avoid an extra round trip",
        "The extracted absolute URL is stored in `og_image_url` on the item row",
        "If the article fetch fails, times out, or no og:image is found, `og_image_url` stays NULL and the item still ingests successfully",
        "Fetch uses the same redirect-handling and User-Agent conventions already used by `001-fix-og-image-redirects` (do not regress that fix)",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 8,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Check BLURHASH_KV in poller; write directly on hit, enqueue on miss",
      "description": "As a developer, I want the poller to read `BLURHASH_KV` for each new item's image URL so that, if another user's poll has already produced a hash for that URL, we can write the hash straight to D1 and skip the queue entirely.",
      "acceptanceCriteria": [
        "KV key derived as `blurhash:<sha256(og_image_url).slice(0, 32)>`",
        "On KV hit: write `blurhash`, `image_width`, `image_height` to the item row in D1; do NOT enqueue",
        "On KV miss: insert the item with `blurhash = NULL` and enqueue `{ imageUrl, itemId }` onto `blurhash-jobs`",
        "If the item has no `og_image_url`, no KV read and no enqueue happens",
        "Hot-path KV read is exactly one round trip per new item",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 9,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-010",
      "title": "Implement blurhash-jobs consumer happy path (fetch, decode, encode, write)",
      "description": "As a developer, I need a queue consumer worker that takes a job, safely fetches the image, decodes and resizes it with @cf-wasm/photon, encodes a BlurHash, captures intrinsic dimensions, and writes the result to KV and D1.",
      "acceptanceCriteria": [
        "Consumer registered for the `blurhash-jobs` queue using the bindings from US-007",
        "On batch receive, for each job: re-check `BLURHASH_KV` first (another worker may have hashed it); on hit, write hash + dimensions to D1 and ack without re-encoding",
        "Image fetch uses `AbortController` with a 10 second timeout",
        "Image fetch uses a browser-like User-Agent header",
        "Reject images with `Content-Length` greater than 5 MB before reading the body",
        "Backstop check rejects images whose actual received bytes exceed 5 MB",
        "Decode → resize to 32×32 (nearest-neighbor) with Photon → encode with `blurhash` at 4×4 components",
        "Capture `image_width` and `image_height` from the original decoded image (before resize)",
        "Photon image instance's `.free()` is called in a `finally` block",
        "Write hash to `BLURHASH_KV` with 90-day TTL",
        "Write `blurhash`, `image_width`, `image_height` to the item's row in D1",
        "Successful jobs ack the message",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-011",
      "title": "Implement blurhash-jobs consumer failure-mode policy",
      "description": "As a developer, I need the consumer to map each failure mode to the correct retry/skip/DLQ outcome per the table in `DOCS/PRD-blurhash.md` §7.5, so transient errors retry and permanent errors stop wasting CPU.",
      "acceptanceCriteria": [
        "4xx fetch response → permanent skip; ack the message; item row stays NULL",
        "Decode failure (Photon throws on the bytes) → permanent skip; ack the message; item row stays NULL",
        "Oversized image (Content-Length or actual bytes > 5 MB) → permanent skip; ack the message; item row stays NULL",
        "5xx fetch response → throw to trigger retry",
        "Fetch timeout (AbortController fires) → throw to trigger retry",
        "KV write failure → throw to trigger retry",
        "D1 write failure → throw to trigger retry",
        "Photon crash mid-encode (other than decode-rejection) → throw to trigger retry",
        "After the queue's `max_retries = 3` exhausts, the message lands in `blurhash-dlq` (verified by config, not behavior change)",
        "Permanent-skip cases never enter the retry path",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 11,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-012",
      "title": "Render <blur-hash> in SSR with graceful fallback",
      "description": "As a reader, when I open my feed list I want to see a blurred preview of each article's image immediately, and when the encoder hasn't caught up yet I still want a sensible fallback (no broken UI).",
      "acceptanceCriteria": [
        "For items where `blurhash`, `og_image_url`, `image_width`, and `image_height` are all present: SSR emits `<blur-hash placeholder=\"…\" src=\"…\" width=\"…\" height=\"…\" alt=\"…\">`",
        "For items with `og_image_url` but missing `blurhash`/dimensions: SSR emits a plain `<img src loading=\"lazy\" alt>`",
        "For items with no `og_image_url`: no image element rendered",
        "Fallback `<img>` uses the same intrinsic rendering box (e.g. CSS `aspect-ratio`) as the `<blur-hash>` element so layout does not flicker when the encoder catches up",
        "No new client-side fetch is introduced for the placeholder — the hash is in the initial HTML",
        "No layout shift or flash of blank space on items that have a hash, on a throttled-network reload",
        "Lines stay within 80 columns",
        "Do not change unrelated CSS",
        "Typecheck passes",
        "Lint passes",
        "Verify in browser using dev-browser skill: load the feed list, throttle the network, confirm placeholders paint immediately and crossfade to real images, and confirm fallback `<img>` renders for items missing a hash"
      ],
      "priority": 12,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-013",
      "title": "Add user_state.feed_version counter to per-user DO SQLite",
      "description": "As a developer, I need a single-row counter table in each user's DO SQLite so the worker can detect when feed contents have changed and invalidate the cached HTML.",
      "acceptanceCriteria": [
        "Append `USER_STATE_SQL` export to `src/shared/schema.ts` after `DEAD_LETTER_OUTBOX_SQL`",
        "`USER_STATE_SQL` defines `user_state` table with `id INTEGER PRIMARY KEY CHECK (id = 1)` and `feed_version INTEGER NOT NULL DEFAULT 0`",
        "`USER_STATE_SQL` includes `INSERT OR IGNORE INTO user_state (id, feed_version) VALUES (1, 0)`",
        "Import `USER_STATE_SQL` in `src/server/durable-objects/index.ts` alongside other schema imports",
        "Call `this.sql.exec(USER_STATE_SQL)` in DO init block after `DEAD_LETTER_OUTBOX_SQL` exec",
        "Schema applies cleanly on a fresh DO (no rows yet) and on an existing DO (table created idempotently)",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 13,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-014",
      "title": "Add bumpFeedVersion and getFeedVersion DO helpers",
      "description": "As a developer, I need private helpers on `RsssUserDO` to read and atomically increment `feed_version` so write paths can bump the counter and the worker can read the current version.",
      "acceptanceCriteria": [
        "`bumpFeedVersion()` private method on `RsssUserDO` runs `UPDATE user_state SET feed_version = feed_version + 1 WHERE id = 1 RETURNING feed_version` and returns the new value (or 0 on null)",
        "`getFeedVersion()` private method on `RsssUserDO` runs `SELECT feed_version FROM user_state WHERE id = 1` and returns the value (or 0 on null)",
        "Both helpers are placed near other private helpers like `rowsWritten`",
        "Both helpers use the existing `this.sql.exec(...).one()` pattern",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 14,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-015",
      "title": "Bump feed_version at the three write sites",
      "description": "As a developer, I need each write site that changes the user's feed contents or blurhash data to bump `feed_version` so the cached HTML is invalidated.",
      "acceptanceCriteria": [
        "After the new-items insert loop in `src/server/durable-objects/index.ts` (~line 1602-1617): call `this.bumpFeedVersion()` once if `newItems.length > 0`",
        "Inside `updateBlurhashFromCacheOrQueue` (~line 1791-1823): call `this.bumpFeedVersion()` after the `UPDATE items SET blurhash = COALESCE(...)` exec, before the existing `return`",
        "Inside `POST /internal/blurhash/items/:id` handler (~line 617-649): call `this.bumpFeedVersion()` after the `UPDATE items SET blurhash = ?, ...` exec, before the 204 response",
        "Bumps are unconditional within the `if (newItems.length > 0)` guard for the insert path",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 15,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-016",
      "title": "Add /internal/feed-version and /internal/lazy-html-data DO endpoints",
      "description": "As a developer, I need two internal endpoints on the DO so the worker can read the current `feed_version` cheaply and fetch the version + first-page items in one round-trip on cache miss.",
      "acceptanceCriteria": [
        "`GET /internal/feed-version` returns `{ version: number }` JSON via `c.json()`, value from `this.getFeedVersion()`",
        "`GET /internal/lazy-html-data` returns `{ version: number, items: Item[] }` JSON in a single response (no torn read)",
        "`/internal/lazy-html-data` reads version via `getFeedVersion()` first, then queries items with the existing `ITEM_COLUMNS` constant and the same join shape used by the existing `/items` handler",
        "Items query uses `ORDER BY items.pub_date DESC, items.id DESC LIMIT 50`",
        "Both endpoints registered next to the existing `/internal/blurhash/items/:id` route in the DO Hono app",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 16,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-017",
      "title": "Add lazy-html generator with safe JSON bootstrap injection",
      "description": "As a developer, I need a pure HTML generator module with cache-key, JSON serializer, and shell-injection helpers, plus tests, so the lazy-html handler can be built on a fully unit-tested foundation.",
      "acceptanceCriteria": [
        "Create `src/server/lazy-html.ts` exporting `buildLazyHtmlCacheKey(did, version)`, `serializeInitialFeed(payload)`, `injectInitialFeed(html, payload)`, and `InitialFeedPayload` interface",
        "`buildLazyHtmlCacheKey` returns `html:${did}:${version}` (deterministic, version-keyed)",
        "`serializeInitialFeed` returns `JSON.stringify(payload).replace(/</g, '\\\\u003c')` so `</script>` cannot break out",
        "`injectInitialFeed` inserts `<script id=\"initial-feed\" type=\"application/json\">{json}</script>` before `</head>`; returns input unchanged when `</head>` is absent",
        "`InitialFeedPayload` has `version:number`, `items:Item[]`, `has_more:boolean`",
        "Create `test/lazy-html.ts` with tapzero tests covering: cache-key determinism + version-keying, serialize round-trip via JSON.parse, `<` escaping prevents script-tag breakout, injectInitialFeed inserts a parseable bootstrap before `</head>`, idempotent when `</head>` missing",
        "Add `test:lazy-html` script to `package.json` using `esbuild ./test/lazy-html.ts --bundle | tapout`",
        "`npm run test:lazy-html` passes (5 tests)",
        "Typecheck passes",
        "Lint passes",
        "Tests parse JSON out of the injected tag rather than asserting on raw HTML text shapes (per project test-brittleness guidance)"
      ],
      "priority": 17,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-018",
      "title": "Provision HTML_KV namespace binding in wrangler config",
      "description": "As a developer, I need an `HTML_KV` Workers KV namespace bound to the worker so the lazy-html handler can cache rendered shells per (did, version).",
      "acceptanceCriteria": [
        "`HTML_KV` entry added to `kv_namespaces` array in `wrangler.jsonc` next to `BLURHASH_KV`",
        "Both `id` and `preview_id` populated with values from `npx wrangler kv namespace create HTML_KV` and `--preview`",
        "`worker-configuration.d.ts` regenerated via `npx wrangler types` (or hand-edited) so `Env` includes `HTML_KV: KVNamespace`",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 18,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-019",
      "title": "Add lazy-html handler with KV+DO cache lookup",
      "description": "As a developer, I need a handler module that reads the DO's current `feed_version`, looks up `HTML_KV` for a cached shell, and on miss fetches first-page data + the static shell, injects the bootstrap, writes to KV, and returns the body.",
      "acceptanceCriteria": [
        "Create `src/server/lazy-html-handler.ts` exporting `handleLazyHtmlRequest(input)` and `LazyHtmlInput` interface",
        "`LazyHtmlInput` has `did`, `kv`, `doStub`, `assets`, `request` fields with appropriate types",
        "Non-HTML / non-GET requests bypass to `assets.fetch(request)` (`accept` header check)",
        "On HTML GET: fetch `/internal/feed-version` from `doStub`; on non-OK fall through to `assets.fetch`",
        "Build cache key via `buildLazyHtmlCacheKey(did, version)`; on KV hit return body with `content-type: text/html;charset=utf-8` and skip the data endpoint and shell fetch",
        "On KV miss: fetch `/internal/lazy-html-data`, fetch `/index.html` via `assets.fetch`, inject payload via `injectInitialFeed`, store in KV with 30-day `expirationTtl`, return body",
        "Payload `has_more` set to `data.items.length >= 50`",
        "Create `test/lazy-html-handler.ts` with tapzero tests covering: cache miss writes correct key + injects bootstrap, cache hit returns body verbatim and does NOT call data endpoint or put, version bump invalidates (new key written, data endpoint called), non-HTML accept bypasses lazy logic",
        "Tests use minimal KV/DO/ASSETS stubs and assert on observable side-effects (KV calls, DO fetch list) rather than raw HTML text shape",
        "Add `test:lazy-html-handler` script to `package.json`",
        "`npm run test:lazy-html-handler` passes (4 tests)",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 19,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-020",
      "title": "Wire lazy-html handler into worker route table",
      "description": "As a developer, I need the worker's catch-all route to consult the lazy-html handler for authenticated HTML GETs so signed-in users get the embedded bootstrap and anonymous users still get the bare static shell.",
      "acceptanceCriteria": [
        "Import `handleLazyHtmlRequest` at top of `src/server/index.ts`",
        "Replace catch-all `app.all('*', ...)` (~lines 1465-1472) with logic that: returns `c.notFound()` if no `ASSETS`, reads `session?.did`, and if `did && c.env.HTML_KV && c.env.USER_DO` calls `handleLazyHtmlRequest` with `getRsssUserDO(c.env, did)` as `doStub`",
        "Anonymous requests still fall through to `c.env.ASSETS.fetch(c.req.raw)` (no DO call)",
        "Reuse the existing helper for resolving the user DO stub (e.g. `getRsssUserDO`) — confirm exact name/import",
        "Non-HTML asset requests (`.js`, `.css`, `.png`) still served by ASSETS via the handler's internal `wantsHtml` guard",
        "Smoke test: authenticated `curl -H 'cookie: session=...' -H 'accept: text/html' /` response contains `<script id=\"initial-feed\" type=\"application/json\">` with feed JSON",
        "Smoke test: unauthenticated `curl -H 'accept: text/html' /` returns shell without the bootstrap script",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 20,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-021",
      "title": "Consume window.__INITIAL_FEED__ on first paint in client state",
      "description": "As a user, I want the first feed paint to render synchronously from the embedded bootstrap so blurhash placeholders appear before the JS bundle finishes its first /api/items round-trip.",
      "acceptanceCriteria": [
        "Create `src/client/initial-feed.ts` exporting `consumeInitialFeed()`, `readInitialFeedFromDom()`, `_resetConsumedForTests()`, and `InitialFeedPayload` interface (with `Window` augmentation for `__INITIAL_FEED__`)",
        "`readInitialFeedFromDom` parses `#initial-feed` script `textContent` as JSON; returns null if missing or unparseable",
        "`consumeInitialFeed` returns the global once (deleting it after read) then falls through to DOM; subsequent calls return null (module-level `consumed` flag)",
        "Create `test/initial-feed.ts` with tapzero tests covering: DOM read parses payload, returns null when absent, global is read-and-cleared, falls through to DOM when global missing — using `_resetConsumedForTests()` between cases",
        "Add `test:initial-feed` script to `package.json`",
        "`npm run test:initial-feed` passes (4 tests)",
        "In `src/client/state.ts` `State.loadItems` (~lines 1491-1515): call `consumeInitialFeed()` first; if it returns a payload with items, set `state.items`, `state.itemsTotal`, `state.itemsLoading=false` inside `batch()` from `@preact/signals` and return without the API call",
        "Existing API-fetch path preserved as fallback (when bootstrap absent or empty)",
        "`recomputeCacheStatus(state).catch(() => {})` still called on the bootstrap path",
        "Lines stay within 80 columns",
        "Use the existing TypeScript style (no space between colon and type annotation)",
        "Typecheck passes",
        "Lint passes",
        "Verify in browser using dev-browser skill: throttle network, hard-reload `/` while signed in — blurhash placeholders for first-page items paint before the JS bundle loads"
      ],
      "priority": 21,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-022",
      "title": "Register lazy-html and initial-feed test scripts in npm test",
      "description": "As a developer, I need the three new test scripts wired into the all-tests runner so `npm test` covers them in CI and locally.",
      "acceptanceCriteria": [
        "`test/run-all-tests.mjs` lists `test:lazy-html`, `test:lazy-html-handler`, and `test:initial-feed` alongside the existing test scripts, in the same style/place",
        "`npm test` runs all three new suites and they pass",
        "Typecheck passes",
        "Lint passes"
      ],
      "priority": 22,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-023",
      "title": "End-to-end sanity verification of lazy-html cache pipeline",
      "description": "As a developer, I want to verify the full pipeline (DO version bump → KV cache → client bootstrap consumption) works in dev so we can ship with confidence.",
      "acceptanceCriteria": [
        "After resetting local state and starting dev, the response body for `/` (signed in) contains a `<script id=\"initial-feed\" type=\"application/json\">` with at least one item carrying a non-empty `blurhash`",
        "A second hard reload at the same `feed_version` does NOT log a hit on `/internal/lazy-html-data` (cache hit path)",
        "After ingesting a new item with `og_image_url`, a new `HTML_KV` key `html:{did}:{newVersion}` is written on the next request (verified via `wrangler kv list`)",
        "Anonymous reload of `/` returns the bare shell with no `id=\"initial-feed\"` script and no DO call",
        "On Slow 3G throttling, blurhash placeholders for first-page items are visible before the JS bundle finishes loading",
        "Typecheck passes",
        "Lint passes",
        "Verify in browser using dev-browser skill: walk through signed-in cache-miss, signed-in cache-hit, post-ingest invalidation, and anonymous fallthrough"
      ],
      "priority": 23,
      "passes": true,
      "notes": ""
    }
  ]
}
