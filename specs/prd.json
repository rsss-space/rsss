{
  "project": "RSSS",
  "branchName": "ralph/production-readiness-audit",
  "description": "Production-Readiness Audit Remediation (P0 + P1) — close the audit gaps before public launch: Stripe/Autumn webhook handling with signature verification and de-dup, entitlement cache invalidation, split wrangler envs, R2-backed account deletion, server-side verified-email flow, idempotency keys on Stripe mutations, per-DID/per-IP/per-handle rate limiting, full security-header set + CSP, SSRF blocklist extensions, Sentry coverage of swallowed errors, PII scrubbing, and a sweep of P1 frontend a11y / lazy-load / preconnect fixes. Tracks tasks/prd-production-readiness-audit.md.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Fix failing lint on staging",
      "description": "As a developer, I want `npm run lint` to pass on the `staging` branch so CI signals real failures again.",
      "acceptanceCriteria": [
        "Remove or use the unused `waitFor` import at `test/feed-create.ts:81`",
        "`npm run lint` exits 0 on `staging`",
        "No new lint warnings introduced",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Split wrangler.jsonc into staging and production env blocks",
      "description": "As an operator, I want separate env blocks in wrangler.jsonc so a `wrangler deploy` cannot ship dev-login or dev-billing to production.",
      "acceptanceCriteria": [
        "Add `[env.staging]` and `[env.production]` blocks to `wrangler.jsonc` with their own KV namespace ids and DO bindings (shared bindings only if intentional and documented)",
        "Remove `NODE_ENV: \"development\"` from the top-level `vars`; each env block sets its own `NODE_ENV`",
        "Update deploy scripts in `package.json` to use `wrangler deploy --env staging` and `wrangler deploy --env production`",
        "Bare `wrangler deploy` either errors out or is removed from documented workflow",
        "Add a predeploy check (script or CI step) that asserts `NODE_ENV=production` before a production deploy and fails otherwise",
        "`canUseDevBillingShortcut` and `/api/auth/dev-login` return 404 under the production env (documented smoke test)",
        "Typecheck and lint pass"
      ],
      "priority": 2,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Pin Stripe SDK and set explicit apiVersion",
      "description": "As an operator, I want Stripe SDK upgrades to be deliberate so a minor bump cannot ship behavior changes without review.",
      "acceptanceCriteria": [
        "`package.json` pins `stripe` to an exact version (no caret/tilde)",
        "`getStripe()` in `src/server/stripe-billing.ts` passes an explicit `apiVersion` in the constructor options",
        "`npm install` runs cleanly and the lockfile is updated",
        "Existing billing tests still pass",
        "Typecheck and lint pass"
      ],
      "priority": 3,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "Add reportError helper and replace bare console.error sites in server",
      "description": "As an operator, I want operationally important server failures to reach Sentry so silently swallowed errors stop hiding regressions.",
      "acceptanceCriteria": [
        "Create `src/server/lib/report-error.ts` exporting `reportError(err: unknown, area: string, context?: Record<string, unknown>): void` that calls `Sentry.captureException(err, { tags: { area }, extra: context })` and `console.error(...)`",
        "Replace bare `console.error(err)` sites in `src/server/index.ts`, `src/server/stripe-billing.ts`, `src/server/email.ts`, `src/server/feed-fetch.ts`, and `src/server/auth/*.ts` with `reportError(err, '<area>')`",
        "Pass a stable `area` string per file (e.g. `'billing'`, `'auth'`, `'feed-fetch'`, `'email'`)",
        "Add a unit test verifying `reportError` calls `Sentry.captureException` with the right tags (mock Sentry)",
        "Sentry mock is wired into the test harness so production assertions do not double-fire in tests",
        "Typecheck and lint pass"
      ],
      "priority": 4,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Build rate-limit middleware factory and apply per-DID buckets",
      "description": "As an operator, I need a single rate-limit middleware factory applied to authenticated edge endpoints so one user cannot fan out unbounded billing, refresh, or admin calls.",
      "acceptanceCriteria": [
        "Add `src/server/middleware/rate-limit.ts` exporting a Hono middleware factory parameterized by bucket size, window seconds, and a key-resolution function",
        "Token-bucket state is stored in KV with a TTL equal to the window",
        "Apply a per-DID 30/min bucket to `/api/billing/*`, `/api/feeds/refresh`, `/api/feeds/:id/refresh`, `/api/items/:id/fetch-full`, `/admin/*`",
        "Over-limit responses return 429 with `Retry-After` header and a generic body that does not leak bucket internals",
        "Regression test: 31st request inside the window returns 429; first request in the next window returns 200",
        "Typecheck and lint pass"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "Apply per-IP and per-handle rate limits to /api/auth/login",
      "description": "As an operator, I want both per-IP and per-handle buckets on the OAuth-start endpoint so an unauthenticated caller cannot enumerate handles or amplify outbound fetches.",
      "acceptanceCriteria": [
        "Apply the rate-limit middleware twice to `/api/auth/login`: per-IP bucket keyed off `cf-connecting-ip` at 10/min, and per-handle bucket keyed off the lowercased `handle` field from request input at 10/min",
        "Both buckets must pass for the request to proceed; either over-limit returns 429",
        "Per-handle bucket key normalizes by trimming and lower-casing the handle before hashing",
        "Regression test: 11 attempts from the same IP with different handles → 429 (per-IP exhausted)",
        "Regression test: 11 attempts against the same handle from different IPs → 429 (per-handle exhausted)",
        "Typecheck and lint pass"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "Scrub PII (DIDs and emails) from server logs",
      "description": "As a privacy-conscious operator, I want server logs free of raw DIDs and email addresses so access to logs is not equivalent to access to PII.",
      "acceptanceCriteria": [
        "Add a `hashDid(did)` helper (first 8 chars of `sha256(did)`) and use it in all log lines that previously emitted raw DIDs",
        "Update `src/server/email.ts:137-141, 160-166` and the `[proxy]` / `[DO]` write-path logs to use the hashed form",
        "Strip email addresses from logs entirely (omit, or replace with the domain part only)",
        "Gate `[proxy]` and `[DO]` write-path logs behind a `LOG_LEVEL=debug` env var; default to `info` in staging/production",
        "Regression test: a billing failure log line does not contain a raw DID or recipient email",
        "Typecheck and lint pass"
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-008",
      "title": "Extend SSRF blocklist (IPv4 CGNAT, benchmark, multicast, reserved + IPv6 multicast)",
      "description": "As the feed and article fetcher, I need to refuse non-public ranges so I cannot scan or attack internal infrastructure.",
      "acceptanceCriteria": [
        "Extend `isBlockedIpv4` in `src/server/feed-fetch.ts:329-364` to add: `100.64.0.0/10`, `198.18.0.0/15`, `224.0.0.0/4`, `240.0.0.0/4`",
        "Extend `isBlockedIpv6` to add `ff00::/8` (multicast)",
        "Regression test: one address inside each new range is rejected",
        "Regression test: an address just outside each range is still accepted (where it would have been before)",
        "Typecheck and lint pass"
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Make DoH the unconditional default in fetchValidatedResponse",
      "description": "As a feed fetcher, I want DoH resolution to be the default whenever a custom fetchFn is passed so a caller cannot silently disable DNS-rebinding protection.",
      "acceptanceCriteria": [
        "In `src/server/feed-fetch.ts:166-168`, default `resolveHostname` to `resolveHostnameWithDoh` whenever `fetchFn` is provided",
        "Opting out requires an explicit `resolveHostname: null` flag (used only in tests)",
        "Update any internal callers that relied on the implicit-undefined behavior",
        "Regression test: passing a custom `fetchFn` without `resolveHostname` results in DoH resolution being attempted",
        "Typecheck and lint pass"
      ],
      "priority": 9,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-010",
      "title": "Derive Secure cookie flag from APP_ORIGIN",
      "description": "As an operator, I want session cookies to be Secure whenever the origin is HTTPS so a misconfigured hostname check cannot drop the flag in production.",
      "acceptanceCriteria": [
        "Rewrite `shouldUseSecureSessionCookie` to return `APP_ORIGIN.startsWith('https://')`, replacing the hostname-based check at `src/server/index.ts:313-315, 707-713, 887-893`",
        "Local dev (`http://localhost:8888`) still issues non-Secure cookies",
        "Regression test: with `APP_ORIGIN=https://example.com`, `Set-Cookie` includes `Secure`; with `APP_ORIGIN=http://localhost:8888`, it does not",
        "Typecheck and lint pass"
      ],
      "priority": 10,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-011",
      "title": "Extend security headers with CSP, XCTO, Referrer-Policy, X-Frame-Options, Permissions-Policy",
      "description": "As a user loading the app, I want full browser-level defenses on by default so XSS and clickjacking surfaces are minimized.",
      "acceptanceCriteria": [
        "Update `src/server/isolation-headers.ts` to add: `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `X-Frame-Options: DENY`, `Permissions-Policy: camera=(), microphone=(), geolocation=()`",
        "CSP directives: `default-src 'self'`; `script-src 'self' https://js.stripe.com`; `connect-src 'self' https://api.stripe.com <SENTRY_INGEST_HOST>` (resolved from `VITE_SENTRY_DSN` at build/start time); `frame-src https://js.stripe.com`; `img-src 'self' data: https:`; `style-src 'self'` (or `'self' 'unsafe-inline'` only if inline styles cannot be eliminated); `base-uri 'none'`; `form-action 'self'`; `frame-ancestors 'none'`",
        "CSP either removes inline scripts or attaches a per-response nonce/hash",
        "Headers applied to HTML responses from the worker",
        "Regression test: response headers from `/` contain each header; Sentry ingest host appears in `connect-src`",
        "Typecheck and lint pass",
        "Verify in browser using dev-browser skill: Stripe Elements still loads in the payment-method modal, Sentry browser SDK posts events successfully, no CSP-violation messages in the console"
      ],
      "priority": 11,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-012",
      "title": "Add R2 bucket binding and back account deletion to R2",
      "description": "As a paying user deleted in error, I want my data exported to R2 before destruction so it can be restored within 30 days.",
      "acceptanceCriteria": [
        "Add R2 bucket binding (`DELETED_ACCOUNTS`) to both `[env.staging]` and `[env.production]` blocks in `wrangler.jsonc`",
        "In `executeAccountDeletion` (`src/server/durable-objects/index.ts:2661-2669`), dump `items`, `feeds`, and `dead_letter_outbox` rows to R2 at key `deleted/<did>/<iso-timestamp>.json` *before* `DROP TABLE` / `storage.deleteAll()`",
        "If R2 write fails, account deletion aborts and the user-facing operation surfaces a retry-able error",
        "Configure lifecycle rule on the bucket to retain exports for 30 days then delete",
        "Document the restore procedure in `README.md`: bucket name, key naming, restore command, expected duration",
        "Regression test: triggering deletion produces an R2 object at the expected key, and the export contains the items rows that were present pre-deletion",
        "Typecheck and lint pass"
      ],
      "priority": 12,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-013",
      "title": "Add idempotency keys to all Stripe mutations",
      "description": "As a user clicking a button twice, I want my second duplicate request to not produce a second Stripe-side mutation.",
      "acceptanceCriteria": [
        "Add `{ idempotencyKey: '<did>:<op>:<resource-id>' }` as the second argument to `paymentMethods.detach`, `customers.update`, `subscriptions.update`, and `setupIntents.create` at `src/server/index.ts:988, 1033, 1060, 1729`",
        "Idempotency key is deterministic for the same logical request and varies for distinct logical requests (the resource id is the payment-method id or subscription id, not a random nonce)",
        "Regression test: the same logical request issued twice produces a single Stripe-side effect (mocked Stripe client asserts only one call with the same idempotencyKey is observed)",
        "Typecheck and lint pass"
      ],
      "priority": 13,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-014",
      "title": "Persist Stripe customer-id → DID mapping at checkout completion",
      "description": "As the webhook handler, I need a durable lookup from Stripe customer id to DID so I can resolve which user an event belongs to.",
      "acceptanceCriteria": [
        "Investigate first whether the existing Autumn customer record or a DO/KV write at checkout-completion time already persists a customer-id → DID mapping. Document the finding in `src/server/CLAUDE.md`",
        "If no durable mapping exists, write a KV index entry `stripe-cust:<customerId>` → `<did>` (no TTL) at the point where a Stripe customer is first created or associated with the DID (likely inside `/api/billing/checkout` or the checkout-return handler)",
        "Expose a helper `getDidForStripeCustomer(customerId): Promise<string | null>` in `src/server/stripe-billing.ts` (or a new `src/server/billing/customer-index.ts`)",
        "Helper returns null gracefully for unknown customer ids (never throws)",
        "Regression test: after a successful checkout-completion, the KV lookup resolves to the expected DID; an unknown customer id resolves to null",
        "Typecheck and lint pass"
      ],
      "priority": 14,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-015",
      "title": "Add POST /api/billing/webhook with signature verification",
      "description": "As the billing system, I need a signature-verified webhook endpoint so external Stripe state changes can reach the worker without trust shortcuts.",
      "acceptanceCriteria": [
        "Add `POST /api/billing/webhook` route in `src/server/index.ts`, declared *before* any JSON-parsing middleware so the raw body is available",
        "Read the raw body via `c.req.raw.text()` and verify with `stripe.webhooks.constructEvent(body, sig, env.STRIPE_WEBHOOK_SECRET)`",
        "Add the route to `isCsrfExemptPath` and bypass session lookup",
        "Signature failures return 400 with no body; missing `Stripe-Signature` header returns 400; successful verification returns 200 (handler body added in a later story is a no-op for now)",
        "Add `STRIPE_WEBHOOK_SECRET` to `.dev.vars.example` and document as a required wrangler secret in `README.md`",
        "Regression test: valid signed event returns 200; tampered body returns 400; missing signature header returns 400",
        "Typecheck and lint pass"
      ],
      "priority": 15,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-016",
      "title": "Implement webhook event routing and entitlement cache invalidation",
      "description": "As a paying user whose subscription is cancelled, refunded, or disputed, I want my entitlement to flip within seconds so paid features stop the moment Stripe says I have stopped paying.",
      "acceptanceCriteria": [
        "Create `src/server/billing/webhook.ts` exporting `handleStripeEvent(event, env, c): Promise<void>`",
        "Handler routes: `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.payment_failed`, `charge.dispute.created`, `charge.refunded`, `payment_method.attached`, `payment_method.detached`",
        "Each routed event resolves the DID via `getDidForStripeCustomer` (US-014) and calls `SESSIONS.delete(billingCacheKey(did))`",
        "Unknown customer id returns 200 (so Stripe does not retry) and logs at warn level via `reportError`",
        "`invoice.payment_failed` additionally triggers `sendPaymentFailed` mail, subject to existing dedupe rules in `src/server/email.ts`",
        "After cache delete, the next `requireEntitlement` call refetches from Autumn and returns the current state",
        "Regression test: simulated `customer.subscription.deleted` removes the cache entry; subsequent `requireEntitlement` denies access",
        "Regression test: webhook with unknown customer id returns 200 with no `SESSIONS.delete` call",
        "Typecheck and lint pass"
      ],
      "priority": 16,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-017",
      "title": "Add webhook event de-dup via KV",
      "description": "As the webhook handler, I need to ignore Stripe retries so duplicate events do not double-process cache invalidation or mail.",
      "acceptanceCriteria": [
        "Before routing, the handler checks KV for key `stripe-event:<event.id>`; if present, returns 200 and skips side effects",
        "After successful event handling, the handler writes `stripe-event:<event.id>` with a 7-day TTL as the *last* step",
        "Mid-handler failures do not poison the dedupe set (no write on error path)",
        "Regression test: same `event.id` posted twice → second response is 200 with no second `SESSIONS.delete` call",
        "Regression test: failure during handling does not write the dedupe key, so a retry can succeed",
        "Typecheck and lint pass"
      ],
      "priority": 17,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-018",
      "title": "Add verified_email storage to per-user DO",
      "description": "As the email-verification flow, I need server-side storage for the verified email so the address is anchored to the DID and never client-writable.",
      "acceptanceCriteria": [
        "Add a `verified_email` column to the per-user DO profile table (or a small `verified_emails` table keyed by DID — pick one and document)",
        "Add a DO endpoint or method `setVerifiedEmail(email)` writable only from the server worker and `getVerifiedEmail(): string | null` readable likewise",
        "Schema migration applied at DO initialization; no backfill needed",
        "Storage is per-DID and isolated by the existing per-user DO boundary",
        "Regression test: `setVerifiedEmail` then `getVerifiedEmail` round-trips the value; default before set is null",
        "Typecheck and lint pass"
      ],
      "priority": 18,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-019",
      "title": "Add server endpoints for sending and confirming email verification codes",
      "description": "As a user without a verified email on file, I want to receive a verification code by email and confirm it so I can proceed to checkout.",
      "acceptanceCriteria": [
        "Add `POST /api/billing/verify-email/start` which accepts `{ email }`, generates a 6-digit code, stores `verify-email:<did>` → `{ codeHash, email, expiresAt }` in KV with a 10-minute TTL, and sends the code via Resend",
        "Add `POST /api/billing/verify-email/confirm` which accepts `{ code }`, compares against the KV entry, and on success calls the DO `setVerifiedEmail` and deletes the KV entry",
        "Codes are stored hashed (`sha256(code)`) — never plaintext",
        "Both endpoints are CSRF-protected and session-authenticated",
        "Both endpoints are rate-limited per DID using the middleware from US-005 (5/min)",
        "Codes expire after 10 minutes; expired codes return a generic 400",
        "Wrong code returns 400; repeated wrong attempts (>= 5) invalidate the code",
        "Regression test: start → confirm with correct code persists the verified email; wrong code does not; expired code does not",
        "Typecheck and lint pass"
      ],
      "priority": 19,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-020",
      "title": "Add client UI for the email verification flow",
      "description": "As a user starting checkout without a verified email, I want a clear in-app flow to enter my email and confirm the code so I can complete the purchase.",
      "acceptanceCriteria": [
        "Add a `<VerifyEmailModal>` (or route component) that presents two steps: enter email (calls `/api/billing/verify-email/start`) and enter code (calls `/api/billing/verify-email/confirm`)",
        "Built on `@substrate-system/dialog`, consistent with the existing payment-method modal",
        "State managed via `@preact/signals`; sequential signal updates use `batch()`",
        "Errors from the start/confirm endpoints are surfaced inline; rate-limit 429 shows a 'try again soon' message",
        "On successful confirmation, the modal closes and the checkout call is retried automatically",
        "Lines stay within 80 columns; uses existing CSS variables (no new color tokens)",
        "Typecheck and lint pass",
        "Verify in browser using dev-browser skill: end-to-end flow from checkout start → verify-email modal → enter code → return to checkout works without a full page reload"
      ],
      "priority": 20,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-021",
      "title": "Block /api/billing/checkout when no verified email; remove client-supplied email from body",
      "description": "As a logged-in attacker, I must not be able to route checkout mail to an arbitrary recipient by passing a chosen `email` in the request body.",
      "acceptanceCriteria": [
        "`/api/billing/checkout` (`src/server/index.ts:1252-1336`) no longer reads or trusts `email` from the request body",
        "Handler calls DO `getVerifiedEmail()`; if null, returns a structured response (e.g. `{ code: 'EMAIL_VERIFICATION_REQUIRED' }`) that the client uses to open the verify-email modal",
        "If a verified email is present, it is used as the Autumn customer email and as the mail destination",
        "No code path stores a client-supplied email on the Autumn customer record",
        "Regression test: POST `/api/billing/checkout` with `{ email: 'attacker@example.com' }` does not result in any outbound mail to `attacker@example.com`",
        "Regression test: a DID without a verified email cannot reach Stripe checkout — the response routes the user to verification instead",
        "Typecheck and lint pass"
      ],
      "priority": 21,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-022",
      "title": "Add aria-labelledby to payment-method modal",
      "description": "As a screen-reader user, I want the payment-method dialog to announce its title when it opens.",
      "acceptanceCriteria": [
        "Add `aria-labelledby=\"payment-method-modal-title\"` (or use the existing `TITLE_ID` constant at line 51) to the `<modal-window>` element at `src/client/components/payment-method-modal.ts:303`",
        "The element referenced by `TITLE_ID` exists in the rendered tree and contains the dialog title",
        "Typecheck and lint pass",
        "Verify in browser using dev-browser skill: macOS VoiceOver (or equivalent) announces the modal title when the modal opens"
      ],
      "priority": 22,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-023",
      "title": "Lazy-load PaymentMethodModal in Settings",
      "description": "As a Settings visitor who never opens the payment-method modal, I want Stripe.js and the dialog component out of my initial JS bundle.",
      "acceptanceCriteria": [
        "Remove the static import of `PaymentMethodModal` at `src/client/routes/settings.ts:55-56`",
        "`handleOpenPaymentMethods` performs a dynamic `import()` of the modal module on first invocation and caches the resolved module for subsequent opens",
        "The modal element is only rendered into the DOM after the dynamic import resolves (no eager `<PaymentMethodModal />` at `settings.ts:875-878`)",
        "Build output: a separate chunk exists for the payment-method modal; Stripe.js is not in the Settings entry chunk",
        "Build assertion or grep against the dist bundle confirms `js.stripe.com` is absent from the Settings entry chunk",
        "Typecheck and lint pass",
        "Verify in browser using dev-browser skill: Settings page network tab shows no Stripe.js request until the modal is opened"
      ],
      "priority": 23,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-024",
      "title": "Add preconnect and dns-prefetch for js.stripe.com",
      "description": "As a user opening the payment-method modal, I want the TLS handshake to Stripe to start in parallel with the dynamic import.",
      "acceptanceCriteria": [
        "Add `<link rel=\"preconnect\" href=\"https://js.stripe.com\" crossorigin>` to `index.html`",
        "Add `<link rel=\"dns-prefetch\" href=\"https://js.stripe.com\">` as a fallback",
        "Tags placed in `<head>` before any module scripts",
        "Typecheck and lint pass",
        "Verify in browser using dev-browser skill: network waterfall shows the preconnect/dns-prefetch firing on initial page load, before the modal is opened"
      ],
      "priority": 24,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-025",
      "title": "Create ConfirmDialog component on @substrate-system/dialog",
      "description": "As a developer, I want a reusable confirm-dialog component so destructive flows can replace native `confirm()` consistently.",
      "acceptanceCriteria": [
        "Add `src/client/components/confirm-dialog.ts` exporting `<ConfirmDialog>` built on `@substrate-system/dialog`",
        "Props: `title`, `message`, `confirmLabel`, `cancelLabel`, `onConfirm: () => Promise<void> | void`, `onCancel?: () => void`, `destructive?: boolean`",
        "Escape key cancels; focus is trapped while open; focus returns to the trigger element on close",
        "Destructive variant styles the confirm button using existing danger-color CSS variables",
        "State managed with `@preact/signals`; sequential signal updates use `batch()`",
        "Lines stay within 80 columns; uses existing CSS variables (no new color tokens)",
        "Typecheck and lint pass",
        "Verify in browser using dev-browser skill: a smoke harness/route renders ConfirmDialog, Escape cancels, confirm runs the callback, focus returns correctly"
      ],
      "priority": 25,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-026",
      "title": "Replace confirm()/alert() in settings.ts with ConfirmDialog",
      "description": "As a user on a screen reader or polished UI, I want billing and destructive flows in Settings to use the in-app dialog instead of native modals.",
      "acceptanceCriteria": [
        "Replace each `confirm()` / `alert()` call in `src/client/routes/settings.ts:117, 160, 193, 202, 250, 259, 270, 435` with `<ConfirmDialog>` invocations",
        "Each replacement preserves the original copy and the original destructive vs informational intent",
        "No new native `confirm()` or `alert()` calls remain in `settings.ts`",
        "Typecheck and lint pass",
        "Verify in browser using dev-browser skill: each replaced flow opens the in-app dialog; Escape cancels; confirm performs the destructive action"
      ],
      "priority": 26,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-027",
      "title": "Replace confirm()/alert() in sidebar.ts and cache-settings.ts",
      "description": "As a user, I want the remaining native modals in the sidebar and cache-settings to use the in-app dialog.",
      "acceptanceCriteria": [
        "Replace the `confirm()` / `alert()` calls at `src/client/components/sidebar.ts:38` and `src/client/components/cache-settings.ts:108` with `<ConfirmDialog>` invocations",
        "Preserve original copy and intent",
        "No native `confirm()` or `alert()` remain in `sidebar.ts` or `cache-settings.ts`",
        "Typecheck and lint pass",
        "Verify in browser using dev-browser skill: removing a feed (sidebar) and clearing the cache (cache-settings) both open the in-app dialog"
      ],
      "priority": 27,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-028",
      "title": "localStorage cleanup: clear rsss_checkout_email on logout and gate DEBUG setItem on diff",
      "description": "As a user on a shared device, I want my checkout email cleared on logout, and I do not want the DEBUG localStorage value rewritten on every page load.",
      "acceptanceCriteria": [
        "Either move `rsss_checkout_email` to `sessionStorage` (`src/client/state.ts:86, 236-258`) or remove the key inside `State.logout()`",
        "Regression test: after `State.logout()`, the storage key is absent",
        "Gate `localStorage.setItem('DEBUG', …)` at `src/client/index.ts:34-40` on a current-value diff (read existing, only write if it differs)",
        "Sequential signal/storage updates inside `State.logout()` use `batch()` where applicable",
        "Typecheck and lint pass"
      ],
      "priority": 28,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-029",
      "title": "Small a11y polish: label Add-feed input, aria-label page-size select, type=button on logout",
      "description": "As a screen-reader user, I want the remaining unlabeled inputs and the logout buttons to be properly described and typed.",
      "acceptanceCriteria": [
        "Add `<label class=\"visually-hidden\" for=\"new-feed-url\">Feed URL</label>` before the input at `src/client/components/sidebar.ts:123-129`",
        "Ensure `.visually-hidden` utility class exists in CSS (add to a shared stylesheet only if missing — do not change unrelated CSS)",
        "Add `aria-label=\"Items per page\"` to the page-size `<select>` at `src/client/routes/feed-reader.ts:225-234`",
        "Add `type=\"button\"` to the logout buttons at `src/client/components/header.ts:96, 127`",
        "Typecheck and lint pass",
        "Verify in browser using dev-browser skill: screen reader announces 'Feed URL' on the sidebar input and 'Items per page' on the select; logout buttons do not submit any ancestor form"
      ],
      "priority": 29,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-030",
      "title": "Add /api/health?deep=1 with KV + DO + Autumn probes",
      "description": "As an operator running uptime checks, I want a deep health probe that exercises real dependencies so half-broken state shows up before users notice.",
      "acceptanceCriteria": [
        "Extend `/api/health` (`src/server/index.ts:475-488`) so that `?deep=1` performs: a KV write+read+delete on a throwaway key; a DO ping (a no-op method on a system DO); an Autumn read-only call",
        "Each probe is bounded by a 2s budget",
        "Returns 200 with `{ kv: 'ok', do: 'ok', autumn: 'ok' }` only when all green; otherwise returns 503 with the failing dependency named",
        "`/api/health` without `?deep=1` keeps its current env-presence-only behavior",
        "Regression test: with mocked KV failure, `?deep=1` returns 503 naming `kv`; with all green, returns 200",
        "Typecheck and lint pass"
      ],
      "priority": 30,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-031",
      "title": "Backfill tests for /api/auth/callback",
      "description": "As a developer, I want regression coverage for the OAuth callback so future changes do not silently break login.",
      "acceptanceCriteria": [
        "Add `test/auth-callback.ts` covering: happy path (valid state, valid code → session cookie set, redirect to app)",
        "Add coverage for state-mismatch (KV state-id missing or different) → error response, no cookie set",
        "Add coverage for replay (same state used twice) → second attempt rejected",
        "Mock upstream PDS / DoH where needed via existing test helpers",
        "`npm test` passes; new tests fail before the fix landed and pass now",
        "Typecheck and lint pass"
      ],
      "priority": 31,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-032",
      "title": "Backfill tests for the Stripe webhook handler",
      "description": "As a developer, I want regression coverage for webhook signature verification, dedupe, and entitlement invalidation so the highest-risk handler stays correct.",
      "acceptanceCriteria": [
        "Add `test/billing-webhook.ts` covering: valid signed event for `customer.subscription.deleted` invalidates the entitlement cache",
        "Tampered body returns 400 with no cache delete",
        "Missing `Stripe-Signature` header returns 400",
        "Duplicate `event.id` returns 200 on the second call with no second cache delete",
        "Unknown customer id returns 200 with `reportError` called at warn level",
        "`npm test` passes",
        "Typecheck and lint pass"
      ],
      "priority": 32,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-033",
      "title": "Backfill tests for /api/billing/checkout/return live path",
      "description": "As a developer, I want coverage for the live checkout-return code path so end-to-end checkout cannot regress silently.",
      "acceptanceCriteria": [
        "Add `test/billing-checkout-return.ts` covering: successful return → entitlement cache populated, customer-id index written",
        "Cancelled return → no entitlement granted, appropriate response",
        "Tampered session_id → request rejected",
        "Mocks for Stripe and Autumn use existing fixture patterns",
        "`npm test` passes",
        "Typecheck and lint pass"
      ],
      "priority": 33,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-034",
      "title": "Backfill tests for requireAdmin and CSRF middleware",
      "description": "As a developer, I want explicit coverage for the admin gate and CSRF helpers so a regression cannot quietly open up a privilege or origin check.",
      "acceptanceCriteria": [
        "Add `test/require-admin.ts` covering: admin DID → allowed; non-admin DID → 403; missing session → 401",
        "Add `test/csrf-middleware.ts` covering `isCrossOriginStateChange` (positive + negative cases) and `hasValidCsrfToken` (valid token, expired token, missing token, mismatched token)",
        "`npm test` passes",
        "Typecheck and lint pass"
      ],
      "priority": 34,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-035",
      "title": "Backfill tests for Resend retry/dedupe",
      "description": "As a developer, I want regression coverage for the mail retry and dedupe paths so transient Resend failures and duplicate sends cannot regress unnoticed.",
      "acceptanceCriteria": [
        "Add `test/email-retry.ts` covering: transient 5xx from Resend triggers retry; permanent 4xx does not retry; success path emits a single call",
        "Add `test/email-dedupe.ts` covering: duplicate `sendPaymentFailed` for the same DID within the dedupe window is suppressed; outside the window it resends",
        "`npm test` passes",
        "Typecheck and lint pass"
      ],
      "priority": 35,
      "passes": false,
      "notes": ""
    }
  ]
}
